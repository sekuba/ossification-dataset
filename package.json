{
  "name": "ossification-dataset",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "description": "Auditable EVM exploit code-age and loss evidence",
  "scripts": {
    "build": "node scripts/build.mjs",
    "candidates": "node scripts/build-candidates.mjs",
    "check": "node scripts/check.mjs",
    "enrich": "node scripts/enrich.mjs",
    "verify": "node scripts/verify.mjs",
    "test": "node --test test/*.test.mjs && node scripts/build-candidates.mjs --check && node scripts/check.mjs && node scripts/build.mjs --check"
  }
}
